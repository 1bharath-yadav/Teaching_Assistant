# Global configuration for the TDS Teaching Assistant project
# This file will be used for both backend and frontend services

# Project name - used for Docker containers
PROJECT_NAME=tds-teaching-assistant

# Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEVELOPMENT=true  # Set to false in production

# Frontend Configuration
FRONTEND_PORT=3000
NODE_ENV=development  # Set to production in production

# Database and Search
TYPESENSE_URL=http://localhost:8108
TYPESENSE_API_KEY=conscious-field
TYPESENSE_DATA_DIR=./typesense-data

# LLM Provider Configuration
# For OpenAI
OPENAI_API_KEY=ollama  # Replace with your API key in production
OPENAI_BASE_URL=http://localhost:11434/v1  # For Ollama local inference
# OPENAI_BASE_URL=https://api.openai.com/v1  # For OpenAI cloud service

# Logging
LOG_LEVEL=INFO

# CORS Configuration
FRONTEND_URL_DEV=http://localhost:3000
FRONTEND_URL_PROD=https://your-production-domain.com
